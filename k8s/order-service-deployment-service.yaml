apiVersion: v1
kind: Service
metadata: 
  name: order-service-deployment-service
spec:
  selector:
    app: logflow-orderservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer